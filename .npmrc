# pnpm 10 stopped hoisting eslint/prettier packages by default; eslint-config-next (eslintrc format)
# resolves its plugins from the project root, so restore the pnpm 9 behaviour.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
